/**
 * 表格列属性
 */
export default {
  isShow: true, // 判断当前是否显示当前列
  minWidth: null,
  fixed: false, // left, right
  resizable: true, // 对应列是否可以通过拖动改变宽度
  showOverflowTooltip: true,
  align: 'center', // left/center/right
  unit: '', // 单位
};
